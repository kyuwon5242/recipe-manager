# ゲーム要素 設計案(たたき台)

作成日: 2026-09-23(2026-09-23 家族単位方針・料理作成/ランク機能を追記して更新。同日、ガチャ回数・カード消費・ランク閾値の方針確定を反映)
ステータス: **方針確定・実装フェーズA着手可**(BACKLOG.mdの「ゲーム要素」3項目を統合した設計案。`BACKLOG.md`の「対応中」へ移す)

対象BACKLOG項目:
- 旬の食材を使ったトレーディングカード機能
- 食材・栄養素に関するクイズ機能

追加スコープ(2026-09-23レビューで追加):
- カード(食材)を組み合わせて料理を作る機能
- 作った料理の数に応じた家族ランク機能

---

## 1. コンセプト

**目的**: 食育 × ゲーム要素を通じて、大人も子供も「楽しく続けながら」食材・栄養・旬について自然と詳しくなる。

既存機能(レシピ登録・献立作成・買い物リスト)は「作業」寄りのため、ゲーム要素は別モードとして切り出すのではなく、**普段の家事動線の中に軽く顔を出す**ことを基本方針とする。

- 毎日1回だけ触れる「今日のガチャ」「今日のクイズ」で習慣化(ソシャゲのログインボーナス的な設計)
- 図鑑(カード集め)を軸に、栄養・旬・豆知識を"景品の裏側"として自然に提示する
- 集めたカードを実際の料理作成に使うことで、収集(インプット)と実践(アウトプット)をつなげる
- **図鑑・ガチャ・料理作成・ランクはすべて「家族」を主語にする**(個人競争ではなく家族で育てる図鑑)

## 2. ゲームループ

```
        ┌─────────────┐
        │ 今日のクイズに挑戦 │──正解──┐
        └─────────────┘        │
               │不正解でも       ▼
               │解説は見せる  家族の今日のガチャにボーナス回数/確率UP
               ▼               │
        ┌─────────────┐        │
        │ 家族で今日のガチャを引く │◀───────┘
        │  (1日最大3枚+ボーナス) │
        └─────────────┘
               │
               ▼
        カード入手(旬なら高レア率、誰が引いたか記録)
               │
               ▼
        家族の図鑑が埋まっていく(一度埋まった枠は消えない)
               │
               ▼
        手持ちカードを消費して、揃ったレシピを「料理として達成」(1レシピ1回)
               │
               ▼
        達成したレシピの種類数に応じて家族ランクが上がる
```

1日の起点は「クイズ」でも「ガチャ」でもどちらからでも良いが、**クイズに正解するとその日のガチャの確率が優遇される**という連携は既存BACKLOGの記述通り採用する。

## 3. 大人・子供の両対応方針

年齢別モードは作らず、**1枚のカードの中で情報を段階表示**することで両対応する。子供は親アカウントを共同利用する前提のため、ログイン主体を分ける必要はない。

- パッと見: イラスト+食材名+一言(ひらがな中心の豆知識、例:「なつやさいの代表!水分たっぷり」)
- タップして詳細: 旬の時期、産地の傾向、栄養素、大人向けの豆知識・レシピ活用のヒント

クイズも同様に、1問ごとに「かんたん/ふつう/むずかしい」の難易度タグを持たせ、出題時にランダムミックスする(誰が答えても遊べるように、極端に専門的な問題は「むずかしい」に寄せる)。

## 4. 機能設計

### 4.1 旬データ基盤

- `ingredients_master`に `season_months integer[]`(1〜12、複数月可)を追加
- 初期データはAIで一括生成 → 人が目視レビュー(食材マスタ整理と同じ運用パターン)
- 旬月に応じてガチャの出現率を重み付けする基礎データとして使う

### 4.2 カード(図鑑・ガチャ)— 家族共有方式

- カードは食材1つにつき1枚(`ingredients_master`と1:1)。ただし「カード」は教育コンテンツ(イラスト・豆知識・レアリティ)を持つため、運用系の`ingredients_master`とは別テーブルで管理する
- レアリティ: ノーマル/レア/スーパーレア/レジェンド の4段階
  - 旬の食材ほど当選率UP、レジェンドは季節限定食材(例: 松茸、さくらんぼ)中心に希少枠として設定
- **ガチャは家族単位で1日最大3枚**。家族の誰が引いても同じ「今日の残り枚数」を消費する(全員で共有する1つの回数枠)
- クイズ正解によるボーナスは、この基本3枚とは別枠で当日分だけ加算される(翌日に持ち越さない)
- **図鑑も家族単位で1つ**(トレード不要。家族全員で1つの図鑑を埋めていく)
- ただし「どのカードを誰が入手したか」は記録する。1枚1枚の入手履歴を残し、図鑑上で「これはお父さんが引いた」「これは子供が引いた」と分かるようにする(家族内の貢献・思い出として表示)
- **一度図鑑に登録されたカードは消えない**。料理作成でカードを消費して手持ち枚数(`owned_count`)が0になっても、「入手済み」であること自体は図鑑上に残り続ける(下記4.4参照)

### 4.3 クイズ

- 出題カテゴリ: 栄養素/旬/産地/保存方法/調理豆知識
- 1日1問〜数問(乱発を防ぐため上限あり)。**個人単位で解答**でき、家族の複数人がそれぞれ挑戦できる
- 正解 → その日の家族共有ガチャに1回分ボーナス、または当たり確率UP(家族の誰が正解しても家族全体の得になる、協力プレイの構図)
- 不正解でも解説を必ず表示(学びとして成立させる)
- 出題は関連食材カード(`related_ingredient_id`)と紐付け、正解後に該当カードの詳細ページへ誘導できるようにする

### 4.4 料理作成(カードの組み合わせ)

既存の「レシピ」機能と図鑑をつなぐ新機能。1料理あたり食材カード3〜5枚程度を使うイメージを想定し、フェーズAのカード対象食材選定・レシピ側の材料構成もこの粒度感に合わせる。

- 家族が登録済みレシピ(`recipes`)ごとに、そのレシピの材料(`recipe_ingredients`)のうち**カード化されている食材**をどれだけ図鑑に持っているか(`owned_count >= 1`)を判定する(カード化されていない食材、たとえば調味料などは判定対象外にして良い)
- **該当する食材カードを100%持っていないと「料理を作る」は実行できない**(一部欠けていても作れる緩和は行わない)
- 「料理を作る」を確定すると、**使った食材カードはそれぞれ1枚ずつ消費される**(`family_cards.owned_count`を1減算)
- ただし**消費してカードが0枚になっても、図鑑上の「入手済み」表示は消えない**(`first_acquired_at`は変更しない。一度埋めた図鑑欄は永久に埋まったまま)
- **1つのレシピにつき「料理を作る」は家族で1回のみ**(達成型)。一度作ったレシピは「達成済み」として料理図鑑に記録され、以後同じレシピを再度「作る」必要はない(候補一覧からは外れ、達成済み一覧に移る)。実際にそのレシピで何度も料理する行為自体は、既存の献立・買い物リスト機能側でこれまで通り自由に行える(ゲーム上の「達成」とは独立)

### 4.5 家族ランク

- **家族が「達成」したレシピの種類数**(=料理図鑑に載った件数)に応じて、家族単位のランクが上がっていく
- 最上位「ミシュランシェフ☆☆☆」到達の目安は**達成100種**。段階は下記7段階とする(要調整可・ミシュランらしく上位ほど必要数の増分を大きめにした傾斜配分案):

  | ランク | 必要達成種類数 |
  |---|---|
  | 駆け出しシェフ | 0〜9種 |
  | 中堅シェフ | 10〜24種 |
  | ベテランシェフ | 25〜44種 |
  | 料理長 | 45〜69種 |
  | ミシュランシェフ☆ | 70〜84種 |
  | ミシュランシェフ☆☆ | 85〜94種 |
  | ミシュランシェフ☆☆☆ | 95〜100種 |

  (均等割りにする場合は0/14/28/43/57/71/86/100のような案もあるが、後半ほど到達が難しくなる方が「ミシュランの星」らしい希少感が出るため上記の傾斜配分を推奨)
- ランクアップ時は演出(バッジ表示・通知)を入れ、ホーム画面や`/game`ハブに現在のランクを常時表示する
- 図鑑コンプリート率(カテゴリ別・季節別、こちらは食材カードの収集進捗)もあわせて進捗表示の一部とする(料理図鑑の達成率とは別軸の指標として両方見せる)
- 最上位到達には少なくとも100種類のカード充足レシピが必要になるため、フェーズAのレシピ・カード対応付けを進める際は「カード化食材だけで作れるレシピ」を100件以上用意できるか、既存レシピ数を見ながら確認する

## 5. データベース設計(草案)

| テーブル | 主なカラム | 説明 |
|---|---|---|
| `ingredients_master`(既存に追加) | `season_months integer[]` | 旬の月(複数可) |
| `game_cards` | `id`(PK), `ingredient_id`(→ingredients_master, unique), `rarity`(normal/rare/super_rare/legendary), `illustration_url`, `trivia_kids_text`, `trivia_adult_text`, `nutrition_summary`, `created_at` | カードマスタ(教育コンテンツ) |
| `family_cards` | `id`(PK), `family_id`(→families), `card_id`(→game_cards), `owned_count`(0以上、消費で減る), `first_acquired_by`(→profiles), `first_acquired_at`(消費後も不変) | 家族の図鑑(所持カード)。family_id+card_idでunique。行自体は`owned_count`が0になっても削除しない(図鑑上は「入手済み」のまま) |
| `card_acquisitions` | `id`(PK), `family_id`(→families), `card_id`(→game_cards), `acquired_by`(→profiles), `acquired_at` | 入手履歴ログ。「誰が・いつ・何を引いたか」を1件ずつ記録し、図鑑の貢献表示に使う |
| `card_consumptions` | `id`(PK), `family_id`(→families), `card_id`(→game_cards), `family_dish_id`(→family_dishes), `consumed_at` | 料理作成でカードを消費した履歴。どの「料理を作る」でどのカードを何枚使ったかの監査ログ |
| `family_game_profile` | `family_id`(PK, →families), `gacha_date`(date, 日次リセット基準日), `draws_used_today`(int), `bonus_draws_today`(int, クイズ正解で加算・翌日リセット), `updated_at` | 家族単位のガチャ残り回数(基本3枚+ボーナス)を管理。ランクは`family_dishes`の件数から都度算出するため、ここに累計カウンタは持たない |
| `quiz_questions` | `id`(PK), `category`, `difficulty`(easy/normal/hard), `question_text`, `choices`(jsonb), `correct_index`, `explanation_text`, `related_ingredient_id`(→ingredients_master, null可) | クイズ出題データ |
| `quiz_attempts` | `id`(PK), `user_id`(→profiles), `question_id`(→quiz_questions), `is_correct`, `answered_at` | 解答履歴(本人単位。同日再出題防止・正答率集計用) |
| `family_dishes` | `id`(PK), `family_id`(→families), `recipe_id`(→recipes), `created_by`(→profiles), `created_at` | 「料理を作る」の達成記録。**`family_id`+`recipe_id`でunique**(1レシピにつき家族で1回のみ)。件数=料理図鑑の達成種類数=ランクの算出元(`COUNT(*) WHERE family_id = ...`) |

RLSは既存パターン踏襲(`is_family_member()`で家族内のみ閲覧・作成可)。ガチャ・図鑑・料理作成はいずれも家族内なら誰でも操作できる(既存の買い物リストと同じ「家族で共有する」設計に合わせる)。

ガチャの残り回数判定: `gacha_date`が今日でなければ`draws_used_today`/`bonus_draws_today`を0にリセットしてから判定。引ける残り枚数 = `3 + bonus_draws_today - draws_used_today`。

## 6. 画面構成(草案)

| パス | 内容 |
|---|---|
| `/game` | ハブ画面。今日のガチャ/クイズの状態、家族ランク、図鑑進捗、直近入手カード |
| `/game/cards` | 図鑑一覧(カテゴリ・季節フィルタ、未入手はシルエット表示、各カードに「誰が入手したか」を表示) |
| `/game/cards/[id]` | カード詳細(イラスト・栄養素・豆知識・入手履歴・関連レシピへのリンク) |
| `/game/gacha` | ガチャ演出・結果表示(家族の誰が引いても同じ「1日3枚+ボーナス」枠を消費) |
| `/game/quiz` | クイズ出題・解答・解説表示 |
| `/game/cook` | 料理作成画面。未達成レシピをカード充足率順に表示し、「料理を作る」で達成を確定(達成済みレシピは別セクションに表示し、再実行はできない) |

ホーム画面(`page.tsx`の`CARDS`配列)に「今日のガチャ」への導線カードを1枠追加する想定。

## 7. 段階的実装ロードマップ

既存フェーズと同程度の粒度(1フェーズ=規模2〜3程度)に分割する案:

| フェーズ案 | 内容 | 規模目安 |
|---|---|---|
| A(**完了・実機確認済み**) | 旬データ整備(`season_months`追加・AI一括生成)、`game_cards`/`family_cards`/`card_acquisitions`テーブル、図鑑閲覧のみ(ガチャなし、初期カードは管理者が手動投入) | 2〜3 |
| B | ガチャ本体(家族単位1日3枚+クイズボーナス、旬による確率調整、`family_game_profile`) | 2〜3 |
| C | クイズ機能(`quiz_questions`/`quiz_attempts`、正解時のガチャ優遇連携) | 2〜3 |
| D | 料理作成機能(カード充足率判定・`family_dishes`、1レシピ1回の達成制)+ 家族ランク機能(達成種類数に応じた7段階ランク表示・演出) | 2〜3 |

家族内トレード機能は「家族で1つの図鑑を作る」方針のため不要と判断し、スコープから削除。

## 8. カードイラストについて(こちらで用意いただく場合に必要な情報)

**2026-09-23、実機確認を踏まえて確定**: 画像は「食材そのものだけのイラスト」とし、**カード風の縁取りや食材名の文字を画像に描き込まない**(縁取り・レアリティ表現・名前表示はすべてアプリ側で重ねる)。試しに縁取り・名前入りの画像(`public/cards/トマト.jpg`)を配置して実機で見た目を確認したところ、アプリ側の枠・名前表示と二重になることが分かったため、この方針で確定した。このテスト画像は仕様に合わないプレースホルダーなので、差し替え用の本番画像を用意する際に上書きしてもらってよい。

- **想定枚数**: 初期スコープは「旬データを整備した食材」と同数(フェーズAで season_months を付与する食材が対象)。まずは対象食材リストを先に確定してから、必要枚数をお伝えする形でよいか
- **1食材=1枚でOK**: レアリティ(ノーマル/レア/スーパーレア/レジェンド)は同じイラストにアプリ側で枠の色・エフェクトを付けて表現する想定。レアリティ違いの描き分けは不要
- **画像仕様**:
  - 食材そのものだけを描いたイラスト(縁取り・カード風フレーム・食材名の文字は入れない)
  - 正方形(例: 512×512px)。図鑑一覧のグリッドとカード詳細の両方で使い回せるように統一
  - **白背景でよい**(2026-09-26変更)。イラストはカード内の「白い紙の窓」(`#fdfcf8`)に全レアリティで収めて表示するため、白背景の画像がそのまま馴染む。背景透過PNGでも窓の白が透けて同じ見た目になる
  - 未入手時は画像を読み込まずカテゴリアイコンのプレースホルダーを表示する(ネタバレ防止・画像未配置時に壊れたアイコンを出さないため)。入手後は同じ画像をそのまま表示するので、シルエット用の別画像は不要
- **ファイル管理**: Supabase Storageは現状未使用(フェーズ16で廃止)のため、新たに有効化せず`public/cards/`配下に静的ファイルとして配置する案。ファイル名を`ingredients_master`の食材名(またはID)に揃えていただければ、`game_cards.illustration_url`から機械的に紐付けできる
- 上記の方針で問題なければ、フェーズA着手時に対象食材リスト(`season_months`を付与する食材の一覧)を先にお渡しする

## 9. 方針確定メモ

2026-09-23のレビューで下記の通り確定。実装フェーズへ進める。

- ガチャは家族単位で1日最大3枚(クイズ正解ボーナスは別枠加算、翌日リセット)
- 料理作成はカード100%充足が必須条件
- 料理を作るとカードは消費されるが、一度図鑑に登録されたカードの「入手済み」表示は消えない
- **ランクは「達成したレシピの種類数」(distinctなrecipe_id数)を母数とする**。1レシピにつき家族で1回だけ「料理を作る」を実行でき、達成後は同じレシピを再度作る必要がない(候補一覧から外れ、達成済み一覧に移る。`family_dishes`は`family_id`+`recipe_id`でunique)
- ランク段階を7段階に変更: 駆け出しシェフ → 中堅シェフ → ベテランシェフ → 料理長 → ミシュランシェフ☆ → ミシュランシェフ☆☆ → ミシュランシェフ☆☆☆(最上位、達成100種で到達)。閾値は4.5節の表を初期案とする
