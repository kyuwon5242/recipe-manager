# ゲーム要素 設計案(たたき台)

作成日: 2026-09-23
ステータス: **未着手・要レビュー**(BACKLOG.mdの「ゲーム要素」2項目を統合した設計案。合意後に`BACKLOG.md`の「対応中」へ移す)

対象BACKLOG項目:
- 旬の食材を使ったトレーディングカード機能
- 食材・栄養素に関するクイズ機能

---

## 1. コンセプト

**目的**: 食育 × ゲーム要素を通じて、大人も子供も「楽しく続けながら」食材・栄養・旬について自然と詳しくなる。

既存機能(レシピ登録・献立作成・買い物リスト)は「作業」寄りのため、ゲーム要素は別モードとして切り出すのではなく、**普段の家事動線の中に軽く顔を出す**ことを基本方針とする。

- 毎日1回だけ触れる「今日のガチャ」「今日のクイズ」で習慣化(ソシャゲのログインボーナス的な設計)
- 図鑑(カード集め)を軸に、栄養・旬・豆知識を"景品の裏側"として自然に提示する
- 正解・収集などの行動がお互いに影響し合うループを作り、遊びの中に学びを埋め込む

## 2. ゲームループ

```
        ┌─────────────┐
        │ 今日のクイズに挑戦 │──正解──┐
        └─────────────┘        │
               │不正解でも       ▼
               │解説は見せる  ガチャの当たり確率UP
               ▼               │
        ┌─────────────┐        │
        │  今日のガチャを引く │◀───────┘
        └─────────────┘
               │
               ▼
        カード入手(旬なら高レア率)
               │
               ▼
        図鑑が埋まっていく(家族共有)
               │
               ▼
        重複カードは家族内でトレード可能
```

1日の起点は「クイズ」でも「ガチャ」でもどちらからでも良いが、**クイズに正解するとその日のガチャの確率が優遇される**という連携は既存BACKLOGの記述通り採用する。

## 3. 大人・子供の両対応方針

年齢別モードは作らず、**1枚のカードの中で情報を段階表示**することで両対応する。

- パッと見: イラスト+食材名+一言(ひらがな中心の豆知識、例:「なつやさいの代表!水分たっぷり」)
- タップして詳細: 旬の時期、産地の傾向、栄養素、大人向けの豆知識・レシピ活用のヒント

クイズも同様に、1問ごとに「かんたん/ふつう/むずかしい」の難易度タグを持たせ、出題時にランダムミックスする(誰が答えても遊べるように、極端に専門的な問題は「むずかしい」に寄せる)。

## 4. 機能設計

### 4.1 旬データ基盤

- `ingredients_master`に `season_months integer[]`(1〜12、複数月可)を追加
- 初期データはAIで一括生成 → 人が目視レビュー(食材マスタ整理と同じ運用パターン)
- 旬月に応じてガチャの出現率を重み付けする基礎データとして使う

### 4.2 カード(図鑑・ガチャ)

- カードは食材1つにつき1枚(`ingredients_master`と1:1)。ただし「カード」は教育コンテンツ(イラスト・豆知識・レアリティ)を持つため、運用系の`ingredients_master`とは別テーブルで管理する
- レアリティ: ノーマル/レア/スーパーレア/レジェンド の4段階
  - 旬の食材ほど当選率UP、レジェンドは季節限定食材(例: 松茸、さくらんぼ)中心に希少枠として設定
- ガチャは **1日1回・ユーザー単位**(後述の「個人 or 家族」論点あり)
- 図鑑は**家族単位で共有・集計**する(誰が引いても家族全体の図鑑が埋まっていくイメージ)。所有履歴は個人単位でも保持し、「誰が何枚持っているか」は見えるようにする

### 4.3 クイズ

- 出題カテゴリ: 栄養素/旬/産地/保存方法/調理豆知識
- 1日1問〜数問(乱発を防ぐため上限あり)
- 正解 → その日のガチャに1回分ボーナス、または当たり確率UP
- 不正解でも解説を必ず表示(学びとして成立させる)
- 出題は関連食材カード(`related_ingredient_id`)と紐付け、正解後に該当カードの詳細ページへ誘導できるようにする

### 4.4 家族内トレード

- 重複所持カードのみトレード対象にできる(コレクション破壊を防ぐ)
- 申請 → 相手が承諾/拒否のシンプルな1対1フロー(献立エージェントの確認フローと同様、ボタン操作ベース)

### 4.5 進捗・達成要素

- 図鑑コンプリート率の表示(カテゴリ別・季節別)
- 簡易的な実績バッジ(例:「春の食材コンプリート」「7日連続ログイン」)は初期スコープ外とし、後続フェーズの拡張候補とする

## 5. データベース設計(草案)

| テーブル | 主なカラム | 説明 |
|---|---|---|
| `ingredients_master`(既存に追加) | `season_months integer[]` | 旬の月(複数可) |
| `game_cards` | `id`(PK), `ingredient_id`(→ingredients_master, unique), `rarity`(normal/rare/super_rare/legendary), `illustration_url`, `trivia_kids_text`, `trivia_adult_text`, `nutrition_summary`, `created_at` | カードマスタ(教育コンテンツ) |
| `user_cards` | `id`(PK), `user_id`(→profiles), `family_id`(→families), `card_id`(→game_cards), `owned_count`, `first_acquired_at` | 所持カード。家族単位の図鑑集計は`family_id`でGROUP BY |
| `user_game_profile` | `user_id`(PK, →profiles), `last_gacha_at`(date), `bonus_draws_today`(int), `updated_at` | 1日1回制限とクイズ正解ボーナスの管理 |
| `quiz_questions` | `id`(PK), `category`, `difficulty`(easy/normal/hard), `question_text`, `choices`(jsonb), `correct_index`, `explanation_text`, `related_ingredient_id`(→ingredients_master, null可) | クイズ出題データ |
| `quiz_attempts` | `id`(PK), `user_id`(→profiles), `question_id`(→quiz_questions), `is_correct`, `answered_at` | 解答履歴(同日再出題防止・正答率集計用) |
| `card_trades` | `id`(PK), `family_id`(→families), `from_user_id`, `to_user_id`, `offered_card_id`, `requested_card_id`, `status`(pending/accepted/rejected/cancelled), `created_at`, `resolved_at` | 家族内トレード申請 |

RLSは既存パターン踏襲(`is_family_member()`で家族内のみ、`user_cards`/`quiz_attempts`は本人+同一家族まで閲覧可、書き込みは本人のRPC経由を想定)。

## 6. 画面構成(草案)

| パス | 内容 |
|---|---|
| `/game` | ハブ画面。今日のガチャ/クイズの状態、図鑑進捗、直近入手カード |
| `/game/cards` | 図鑑一覧(カテゴリ・季節フィルタ、未入手はシルエット表示) |
| `/game/cards/[id]` | カード詳細(イラスト・栄養素・豆知識・関連レシピへのリンク) |
| `/game/gacha` | ガチャ演出・結果表示 |
| `/game/quiz` | クイズ出題・解答・解説表示 |
| `/game/trade` | 家族内トレード申請一覧・新規申請 |

ホーム画面(`page.tsx`の`CARDS`配列)に「今日のガチャ」への導線カードを1枠追加する想定。

## 7. 段階的実装ロードマップ

既存フェーズと同程度の粒度(1フェーズ=規模2〜3程度)に分割する案:

| フェーズ案 | 内容 | 規模目安 |
|---|---|---|
| A | 旬データ整備(`season_months`追加・AI一括生成)、`game_cards`/`user_cards`テーブル、図鑑閲覧のみ(ガチャなし、初期カードは管理者が手動投入) | 2〜3 |
| B | ガチャ本体(1日1回、旬による確率調整、`user_game_profile`) | 2〜3 |
| C | クイズ機能(`quiz_questions`/`quiz_attempts`、正解時のガチャ優遇連携) | 2〜3 |
| D | 家族内トレード機能(`card_trades`) | 2 |

既存の「献立エージェント」(規模4級)を一括対応した反省も踏まえ、A→Dの順で小さく刻んで進める案。

## 8. 未確定・要確認事項

以下はスキーマ・UXの根幹に関わるため、実装着手前に方針を決めたい。

1. **図鑑・ガチャは個人単位か、家族で1つ共有か**: 上記案は「ガチャは個人単位(毎日1回)・図鑑は家族で集計表示」のハイブリッドだが、この前提で良いか
2. **子供は専用アカウントを持つ想定か**: 現状`family_members`はSupabase Authアカウント前提。子供が自分のメールアドレスでログインする想定か、親のアカウントを一緒に使う想定かでトレード機能・個人別ガチャの意味合いが変わる
3. **カードイラストの調達方法**: AI画像生成(コスト発生、[feedback_recipe_app_workflow](AIコスト実測の方針)に沿って実測が必要)/ フリー素材 / 手描きのどれを想定するか
4. **ロードマップの順序**: A→D(旬データ→図鑑→ガチャ→クイズ→トレード)で良いか、別の優先度があるか
