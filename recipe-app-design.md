# 自炊レシピ管理アプリ(レシピマネージャー) 設計書

作成日: 2026-09-17
最終更新: 2026-09-21(フェーズ13時点)

---

# 現在の仕様(最新版サマリー)

以下は「今このアプリが何であるか」を、変更履歴(1章以降のフェーズ別記録)とは別にまとめたものです。フェーズ別記録は「いつ・なぜ・どう変わったか」の経緯を追うためのもの、こちらは常に最新状態を反映する仕様サマリーという位置づけです(大きな仕様変更をしたら、この章も合わせて更新すること)。

## A. サービス概要・ユースケース

自炊を続ける家族向けの、レシピ管理〜献立決め〜買い物リスト化を一気通貫で行うWebアプリ(サービス名: レシピマネージャー)。個人ではなく「家族」単位でレシピ・買い物リストを共有する。

主なユースケース:

| ユースケース | 主な画面 |
|---|---|
| 作ったレシピを記録する(手入力、またはレシピサイトのURLから自動入力) | `/recipes/new` |
| 過去のレシピを探す・見返す(ジャンル・カテゴリ・食材名で検索、お気に入り) | `/recipes` |
| 「何を作るか」をAIに相談する(自由入力から新規レシピ案を提案してもらう) | `/recipe-suggestions` |
| 「今日の献立一式」をAIに考えてもらう(品目ごとの品数を指定) | `/menu-plan` |
| 決めた献立を画面右の「献立トレイ」に集めて、人数を調整する | 上記2画面 + `/recipes`(共通パネル) |
| 献立から必要な食材を割り出し、手持ちを除いた買い物リストを作る | `/shopping-list` |
| 確定した買い物リストを家族で共有し、買い物中にチェックしていく | `/shopping-lists`, `/shopping-lists/[id]` |
| 家族を作る/招待コードで参加する、招待コードの共有 | `/family/setup`, `/family` |
| (利用許可制・実験機能)会話しながら献立を決め、食材確認〜買い物リスト作成まで進める | `/menu-agent` |
| (管理者)全家族のレシピ・ユーザーを横断管理し、問題があれば利用停止する | `/admin`, `/admin/recipes`, `/admin/users` |
| (管理者)食材マスタの表記ゆれ・カテゴリ誤りをAIで整理する | `/ingredients` |

## B. 画面構成(現在のルーティング一覧)

| パス | 内容 | 認証 |
|---|---|---|
| `/login`, `/signup` | ログイン・新規登録(Google OAuth / メール・パスワード) | 不要 |
| `/auth/callback` | OAuthコールバック | 不要 |
| `/suspended` | 利用停止された旨を表示する案内ページ | 不要 |
| `/family/setup` | 家族の新規作成 / 招待コードで参加 | 要ログイン、家族未所属時 |
| `/recipes` | レシピ一覧(検索・ジャンル/カテゴリ絞り込み・お気に入り・献立トレイ) | 要ログイン |
| `/recipes/new` | レシピ新規登録(URL自動入力対応) | 要ログイン |
| `/recipes/[id]` | レシピ詳細 | 要ログイン |
| `/recipes/[id]/edit` | レシピ編集 | 要ログイン |
| `/recipe-suggestions` | AIによる新レシピ提案 | 要ログイン |
| `/menu-plan` | AIによる献立(品目セット)提案 | 要ログイン |
| `/shopping-list` | 食材リスト作成(レシピ選択→必要な食材確認→下書き→確定) | 要ログイン |
| `/shopping-lists` | 買い物リスト一覧 | 要ログイン |
| `/shopping-lists/[id]` | 買い物リスト詳細(チェックリスト、コピー機能) | 要ログイン |
| `/family` | 家族設定(招待コード、メンバー一覧、名称変更) | 要ログイン |
| `/menu-agent` | 献立エージェント(会話形式で献立決め〜買い物リスト作成、実験機能) | 要利用許可(管理者、または管理者が個別許可したユーザー) |
| `/api/menu-agent` | 献立エージェントのAPI(Route Handler、NDJSONストリーミング) | 要利用許可(同上) |
| `/ingredients` | 食材マスタの表記ゆれ・カテゴリ整理 | 要管理者 |
| `/admin` | 管理者ダッシュボード | 要管理者 |
| `/admin/recipes` | 全家族のレシピ一覧(横断参照・編集) | 要管理者 |
| `/admin/users` | 全ユーザー一覧(表示名編集・利用停止) | 要管理者 |

## C. データベース仕様(現在のスキーマ)

Supabase(PostgreSQL)。全テーブルRLS有効。`auth.users`はSupabase Auth標準テーブル。

| テーブル | 主なカラム | 説明 |
|---|---|---|
| `profiles` | `id`(PK, →auth.users), `email`, `display_name`, `avatar_url`, `is_admin`, `is_suspended`, `can_use_menu_agent`, `created_at` | ユーザープロフィール。サインアップ時に`handle_new_user()`トリガーで自動作成 |
| `families` | `id`(PK), `name`, `invite_code`(unique), `owner_id`(→profiles), `created_at` | 家族。作成・招待参加はRPC(`create_family`/`join_family_with_code`)経由 |
| `family_members` | `family_id`+`user_id`(複合PK), `role`(owner/member), `joined_at` | 家族の所属関係 |
| `recipes` | `id`(PK), `title`, `category`, `genre`, `servings`, `instructions`, `memo`, `recipe_url`, `photo_url`, `is_favorite`, `family_id`(→families), `created_by`/`updated_by`(→profiles), `created_at`, `updated_at` | レシピ本体。家族単位でスコープ |
| `ingredients_master` | `id`(PK), `name`(unique), `default_unit`, `category`, `created_at` | 食材の共有辞書(家族を跨いで共有)。`category`はスーパー準拠の10分類(下記E参照) |
| `recipe_ingredients` | `id`(PK), `recipe_id`(→recipes), `ingredient_id`(→ingredients_master), `quantity`, `unit` | レシピごとの必要食材(数量・単位はレシピ側で保持し、名前はマスタを参照) |
| `shopping_lists` | `id`(PK), `family_id`(→families), `created_by`(→profiles), `title`, `created_at`, `updated_at`, `store_id`(→family_stores, null可) | 確定済み買い物リスト。上書き保存時は`updated_at`が更新される |
| `shopping_list_items` | `id`(PK), `shopping_list_id`(→shopping_lists), `name`, `quantity`, `unit`, `category`, `position`, `is_checked` | 買い物リストの各品目 |
| `family_stores` | `id`(PK), `family_id`(→families), `name`, `category_order`(text[]), `position`, `created_at` | 家族の「よく使うスーパー」(最大10件、アプリ側で制御)。カテゴリの並び順を保持 |
| `family_default_items` | `id`(PK), `family_id`(→families), `name`, `quantity`, `unit`, `category`, `position`, `created_at` | 家族の「どの買い物でも必ず含める食材」 |

主なDB関数・トリガー: `is_family_member()`/`is_family_owner()`/`is_admin()`(RLS内再帰回避用のSECURITY DEFINER関数)、`create_family()`/`join_family_with_code()`(RPC)、`handle_new_user()`(profiles自動作成)、`set_recipe_created_by()`/`set_recipe_updated_by()`(SQL Editor実行時も壊れないようcoalesce対応済み)、`protect_profile_admin_fields()`(非管理者による`is_admin`/`is_suspended`/`can_use_menu_agent`の自己書き換え防止。SQL Editorからの管理者付与は許可)。

マイグレーション一覧: `supabase/migrations/0001_init.sql`〜`0008_fix_admin_bootstrap_trigger.sql`(詳細は5〜14章の各フェーズ記録を参照)。

## D. システム構成(技術要素)

| 要素 | 技術 |
|---|---|
| フロントエンド/バックエンド | Next.js 16(App Router, Turbopack, Server Actions) |
| データベース・認証・ストレージ | Supabase(PostgreSQL, Auth, Storage) |
| AI | Anthropic API。用途によりモデルを使い分け(下記) |
| ホスティング | Vercel(GitHub連携で自動デプロイ) |
| スタイリング | Tailwind CSS v4(`@theme`でブランドカラー`brand-*`をカスタム定義) |

AIモデルの使い分け方針(フェーズ9で整理):

| 用途 | モデル | 理由 |
|---|---|---|
| 新レシピ提案・献立提案 | `claude-opus-5`(effort: medium) | 提案内容の質が体験に直結するため |
| 食材名解決(保存時)・食材一括整理・レシピURL抽出 | `claude-haiku-4-5-20251001` | 機械的な分類・抽出タスクでありフロンティア級の推論力を必要としないため、頻度の高い呼び出しのコストを抑える |
| 献立エージェントの対象外判定(門番) | `claude-haiku-4-5-20251001`(構造化出力) | 本番ループに入る前の安価な足切り。対象外なら高コストなループ自体に入らない |
| 献立エージェントの本体(ツール呼び出しループ) | `claude-opus-5` | 複数ツールを跨いだ計画・会話の質が体験に直結するため |

## E. 食材カテゴリ(スーパー準拠タクソノミー)

`野菜・果物 / 肉 / 魚介 / 卵・乳製品 / 豆腐・大豆製品 / 米・パン・麺 / 調味料・油 / 粉類・乾物・缶詰 / 冷凍食品 / その他`(`src/lib/ingredients/categories.ts`)。買い物リストはこの順で固定表示される。

## F. 機能一覧

- レシピCRUD、お気に入り、URL自動インポート(AI抽出)
- 認証(Google OAuth/メール・パスワード)、家族共有、招待コード
- AI新レシピ提案・AI献立提案(品目別品数指定)
- 献立トレイ(ドラッグ&ドロップ + タップで追加の両対応、localStorage永続、個人ごとの作業スペース)
- 食材リスト作成(単位のバラつき自動統合、カテゴリ一括「持っている」選択、ステップ表示、レシピ選択欄の自動折りたたみ、手持ち確認を省略するクイック作成)
- 買い物リスト(家族共有、チェックリスト、カテゴリ固定順表示、クリップボードコピー)
- 食材名表記ゆれの解消(保存時自動解決 + 管理者限定の一括整理ツール、未分類/旧カテゴリのみを対象にした増分処理)
- 管理者機能(全家族レシピの横断参照・編集、ユーザー管理、利用停止による強制ログアウト)
- セキュリティ対策(セキュリティヘッダー、ファイルアップロード検証、RLS+アプリ層の多層防御、SSRF対策)
- UI/UXブランディング(コック帽アイコン、オレンジ/コーラル配色、AI処理中アニメーション、ボタン押下フィードバック)
- ゾーン別カラーコーディング(機能ごとに色分けしたアイコン・ナビ現在地表示)、生成り色の背景、コンテンツが浮き上がるシャドウ
- 献立エージェント(利用許可制・実験機能。会話しながら献立を決め、買い物リスト作成まで誘導するツール呼び出しエージェント。管理者は個別のユーザーに利用権限を付与できる)
- デプロイバージョンの表示・強制再ログイン(デプロイ後、旧バージョンのクライアントで操作を続けさせない)
- 買い物リストの上書き保存(件数上限に達した場合)、作成・更新日時の表示
- 家族ごとの「どの買い物でも必ず含める食材」設定
- 家族ごとの「よく使うスーパー」設定(食材カテゴリの並び順を保存し、買い物リスト作成時に選択できる)

---

# 変更履歴(フェーズ別記録)

## 1. 概要

日常的な自炊レシピを管理し、必要な食材やメニューを提案するアプリ。
家族で共有し、AIエージェントによる「買い物リストの最適化」を主軸機能とする。

## 2. 要件

| 項目 | 内容 |
|---|---|
| 利用形態 | Webアプリ(PCブラウザ)。将来的にスマホアプリにも展開予定 |
| 開発環境 | ローカルで開発 |
| 運用環境 | インターネット経由(Vercel等)で公開 |
| 利用者 | 家族単位で共有(個人ごとにアカウントを持ち、所属する「家族」でレシピ帳を共有する。詳細は8章) |
| 学習目的 | AIエージェント(複数ステップで判断・行動する仕組み)の実装を学ぶ |

## 3. 技術構成

| 要素 | 採用技術 | 備考 |
|---|---|---|
| フロントエンド | Next.js (React) | Vercelとの相性が良い |
| バックエンド | Next.js API Routes | フロントと同一プロジェクトで完結 |
| データベース | Supabase (PostgreSQL) | 将来のスマホアプリからも同じDBに接続可能 |
| ストレージ | Supabase Storage | レシピ写真の保存に使用 |
| AIエージェント | Anthropic API (Claude) + Tool Use | 複数ステップで動くエージェントとして実装 |
| ホスティング | Vercel | GitHub連携で自動デプロイ |
| 認証 | Supabase Auth(Google OAuth + メール/パスワード)。詳細は8章 |

## 4. 食材リスト機能(旧: 買い物リスト最適化)

フェーズ2ではAIエージェント(Claude + Tool Use)による自然文照合として実装したが、フェーズ4で手持ち食材の入力を「必要な食材一覧からの選択+数量入力」に変更したことに伴い、照合ロジックはAIを使わない決定的な計算に置き換えた(詳細は10章)。現在の動作フロー:

1. 献立を確認する(選択されたレシピを取得)
2. 必要な食材を洗い出す(各レシピの材料をリアルタイムに集計・重複統合)
3. ユーザーが「必要な食材一覧」に対して手持ち分量を入力する(自然文ではなく数量入力)
4. 不足分を「食材リスト」としてカテゴリ分けして提示、編集・並び替え後に「買い物リスト」として確定する

※在庫は常時管理せず、都度入力する運用は維持。AIエージェント(Tool Use)によるフェーズ2の実装は学習目的の実績として残すが、現行の食材リスト生成では使用していない。

### 将来的に追加予定のAIエージェント機能
- 食材ベースのメニュー提案
- レシピの自動登録支援(URL/テキストから材料・手順を解析)
- 栄養バランスのアドバイス
- 対話型の献立相談エージェント

## 5. データベース設計(フェーズ1)

### 5.1 recipes(レシピ)

| カラム名 | 型 | 内容 |
|---|---|---|
| id | UUID | 主キー |
| title | text | レシピ名 |
| category | text | 和食/洋食/中華など(任意) |
| genre | text | 主菜/副菜/汁物など(任意) |
| servings | int | 何人前か |
| instructions | text | 作り方(手順) |
| memo | text | 自由メモ |
| recipe_url | text | 参照元レシピURL(検索用) |
| photo_url | text | 写真URL(Supabase Storage保存先) |
| family_id | UUID | 所属する家族(families）への外部キー ※8章で追加 |
| created_by | UUID | 作成したユーザー(profiles）への外部キー ※8章で追加 |
| updated_by | UUID | 最終更新したユーザー(profiles）への外部キー ※8章で追加 |
| is_favorite | boolean | 家族共有の「いいね」フラグ ※9章で追加 |
| created_at | timestamp | 作成日時 |
| updated_at | timestamp | 更新日時 |

### 5.2 ingredients_master(食材マスタ)

| カラム名 | 型 | 内容 |
|---|---|---|
| id | UUID | 主キー |
| name | text | 食材名(例:玉ねぎ) |
| default_unit | text | 標準単位(個/g/ml等) |
| category | text | 野菜/肉/調味料などの分類 |

### 5.3 recipe_ingredients(レシピごとの必要食材)

| カラム名 | 型 | 内容 |
|---|---|---|
| id | UUID | 主キー |
| recipe_id | UUID | recipesへの外部キー |
| ingredient_id | UUID | ingredients_masterへの外部キー |
| quantity | numeric | 必要量(例:1、200) |
| unit | text | 単位(個、g等) |

### 5.4 stock(在庫)について

常時管理用のテーブルは**作成しない**。
理由:在庫を常に最新に保つ運用コストを避けるため、買い物リスト提案時にユーザーが手持ち食材をその場で入力する形とする。入力内容はエージェントへの一時的なパラメータとして扱う。

## 6. 画面構成(フェーズ1)

1. レシピ一覧画面
2. レシピ詳細画面
3. レシピ登録・編集画面(写真アップロード、URL入力を含む)

※在庫一覧画面は上記理由により不要。

## 7. 開発フェーズ

### フェーズ1(基礎固め)
- レシピの登録・一覧・詳細・編集・削除(CRUD)

### フェーズ2(AIエージェント初挑戦)
- 「選択したレシピ群 → 必要食材の洗い出し → 手持ち食材と照合 → 買い物リスト生成」をエージェントで実装
- まずは最小限のツール(例:照合ツールのみ)から着手

### フェーズ2.5(認証・家族機能) ※実装済み
- Supabase Authによる個人アカウント化と、「家族」単位でのレシピ共有(詳細は8章)

### フェーズ3(拡張) ※メニュー提案・自動登録・いいねは実装済み
- ツールの追加(単位変換、カテゴリ分類など)
- 食材ベースのメニュー提案エージェント → 実装済み(詳細は9章)
- レシピ自動登録支援 → 実装済み(詳細は9章)
- レシピの「いいね」・お気に入り表示 → 実装済み(詳細は9章)

### フェーズ4(食材リスト/買い物リストの刷新) ※実装済み
- 食材リスト作成のUX刷新(レシピ選択のリアルタイム反映、手持ち食材の選択式入力への変更)
- 食材リストの編集・カテゴリ並び替え、確定後の「買い物リスト」への登録・共有・チェックオフ機能
- 詳細は10章

### フェーズ5(一覧UX改善・献立作成) ※実装済み
- レシピ一覧のジャンル別タブ分け・カテゴリ色分け・検索/フィルタ
- 「メニュー提案」を「レシピ提案」に改称
- 「献立作成」機能の新設(主食・主菜・副菜・汁物を1セットで提案し、食材リスト作成に連携)
- 食材リストを作成画面のタブ分け・人数調整・カテゴリ別必要食材表示・チェック式手持ち入力
- 詳細は11章

### フェーズ6(新レシピ提案の刷新、献立提案の改善、献立トレイ、食材整理) ※実装済み
- 「レシピ提案」を「新レシピ提案」に改称し、既存レシピ候補の表示を廃止(新規案のみ)
- 「献立作成」を「献立提案」に改称し、品目ごとの品数指定・人数入力の後工程化に変更
- レシピ一覧・新レシピ提案・献立提案の3画面に「献立トレイ」(ドラッグ&ドロップで主食/主菜/副菜/汁物の枠にレシピを設定する常設パネル)を追加
- 食材リスト作成画面のレシピ選択にスクロール・お気に入り優先表示・カテゴリ色分けを追加
- 食材名の表記ゆれをAIで解消する「食材の整理」機能を追加。レシピ保存時にも新規食材名を既存マスタと照合し、表記ゆれを自動で吸収する
- 詳細は12章

## 8. 認証・家族機能設計(フェーズ2.5)

### 8.1 背景・要件

フェーズ1時点では「認証なし・anonキーで誰でも読み書き可能」だったが、Vercelでの公開にあたりセキュリティ上の懸念があったため、個人アカウント制 + 家族単位でのデータ共有に変更する。

- ログイン単位: 家族一人ひとりに個別アカウント(誰が登録・編集したか記録可能にするため)
- 認証方法: Google OAuth + メール/パスワード(Supabase Auth標準機能)
- 家族単位でレシピ帳を共有するイメージは維持(個人ごとに別々のレシピ帳にはしない)

### 8.2 データベース設計(追加分)

#### profiles(ユーザープロフィール)

`auth.users`と1:1。サインアップ時にトリガーで自動作成される公開用プロフィール。

| カラム名 | 型 | 内容 |
|---|---|---|
| id | UUID | 主キー。auth.users.id と同一 |
| email | text | メールアドレス |
| display_name | text | 表示名(Googleの場合は氏名を自動設定) |
| avatar_url | text | アバター画像URL |
| created_at | timestamp | 作成日時 |

#### families(家族)

| カラム名 | 型 | 内容 |
|---|---|---|
| id | UUID | 主キー |
| name | text | 家族の名前(例:「田中家」) |
| invite_code | text | 招待コード(一意、家族参加に使用) |
| owner_id | UUID | 作成者(profiles）への外部キー |
| created_at | timestamp | 作成日時 |

#### family_members(家族メンバー)

| カラム名 | 型 | 内容 |
|---|---|---|
| family_id | UUID | families への外部キー |
| user_id | UUID | profiles への外部キー |
| role | text | owner / member |
| joined_at | timestamp | 参加日時 |

主キーは (family_id, user_id) の複合キー。

#### recipes への追加カラム

`family_id`(所属家族、NOT NULL)、`created_by`(作成者)、`updated_by`(最終更新者)を追加。既存の `ingredients_master` / `recipe_ingredients` は変更なし(食材名の辞書は家族を跨いだ共有データとして扱う)。

### 8.3 アクセス制御(RLS)の変更

フェーズ1の「anonキーなら誰でも読み書き可」ポリシーを全廃し、以下に変更する。

- `recipes` / `recipe_ingredients`: 認証済みユーザーのうち、そのレシピの `family_id` に `family_members` として所属している人のみ読み書き可能
- `ingredients_master`: 認証済みユーザーであれば誰でも読み書き可能(個人情報を含まない共有辞書のため、家族での区別はしない)
- Storageの `recipe-photos` バケット: アップロードパスを `家族ID/ファイル名` の構成にし、自分の所属家族のフォルダのみ読み書き可能に制限

### 8.4 認証・家族機能の画面構成

| 画面 | 内容 |
|---|---|
| `/login` | Googleログインボタン + メール/パスワードのログインフォーム |
| `/signup` | メール/パスワードでの新規登録 + Googleボタン |
| `/auth/callback` | OAuthコールバック処理。セッション確立後、家族未所属なら `/family/setup` へ |
| `/family/setup` | 初回ログイン時、家族未所属の場合に表示。「新しく家族を作成」または「招待コードで参加」 |
| `/family` | 家族設定画面。家族名・招待コード(コピー可)・メンバー一覧を表示。招待コードの再発行はownerのみ |

### 8.5 招待の仕組み

メール送信基盤を用意しないシンプルな方式として、家族ごとに一意の「招待コード」を発行する。家族のownerがコードを共有し、新しいメンバーはログイン後に `/family/setup` でコードを入力して参加する。

### 8.6 ルーティング保護

Next.js 16の `proxy.ts`(旧middleware)でセッションを毎リクエスト更新し、以下のリダイレクトを行う。

- 未ログイン → `/login`
- ログイン済みだが家族未所属 → `/family/setup`

## 9. メニュー提案・自動登録・いいね機能(フェーズ3)

### 9.1 メニュー提案機能(`/menu-suggestions`)

自由入力(気分・食べたいジャンル・手持ちの食材・季節感など)に応じて、Claudeが以下2種類を提案する。

1. **登録済みレシピからの提案**: 家族のレシピ一覧(タイトル・カテゴリ・ジャンル・材料名)をコンテキストとして渡し、要望に合うものを選ばせる。該当レシピの詳細ページへのリンクを表示。
2. **AIによる新しいレシピ案**: 登録済みレシピで十分でない場合、Claudeがゼロから新しいレシピ案(材料・手順込み)を考案する。各案は「詳細を見て登録する」で展開すると、値が事前入力された登録フォーム(既存の `RecipeForm` を再利用)が現れ、そのまま登録できる。

季節・旬の食材についての要望は、サーバー側で計算した本日の日付をプロンプトに含め、Claudeの一般知識で判断させる(リアルタイムのWeb検索は行わない。将来的に必要になれば`web_search`ツールの追加を検討)。

実装は単発のAI呼び出し(`messages.parse` + Zodによる構造化出力)であり、フェーズ2のようなツール呼び出しループは使用しない。

### 9.2 レシピ自動登録支援(URLから)

`/recipes/new` にURL入力欄を追加。入力されたURLをサーバー側で取得(fetch)し、レシピ名・カテゴリ・ジャンル・人数・材料(名前/数量/単位)・手順を構造化出力(`messages.parse` + Zod)で抽出する。抽出結果は登録フォームに反映され、そのまま確認・修正して登録できる。

- **抽出精度の向上**: 多くのレシピサイトがSEO目的で埋め込んでいる`schema.org/Recipe`形式のJSON-LD構造化データを優先的に読み取り、見つかった場合はそれをClaudeに渡す。見つからない場合はページ本文(HTMLタグ除去後のテキスト、先頭15000文字)にフォールバックする。クックパッド、デリッシュキッチン、楽天レシピ、キッコーマン、ニチレイなど、材料・手順をサーバー側HTMLに含む主要サイトでは概ね機能する見込みだが、JSレンダリングに強く依存するサイトでは精度が下がる場合がある。
- **抽出できなかった場合の制御**: Claude自身に「レシピページと言えるか(`is_recipe_page`)」を判定させ、falseまたは材料が1件も取れなかった場合は警告(「十分な情報を抽出できませんでした。内容をご確認のうえ手動で入力してください」)をUIに表示し、空欄のまま手動入力に切り替えられるようにする。
- 対応範囲: http/https のURLのみ。ローカルアドレス・プライベートIPへのアクセスは拒否(SSRF対策)

### 9.3 レシピの「いいね」

`recipes.is_favorite`(boolean)を家族全体で共有するフラグとして追加。一覧・詳細画面にハートボタンを設置し、誰か1人がいいねすると家族全員の画面に反映される。一覧画面には「お気に入りのみ表示」フィルタ(`?favorite=1`)を用意。

## 10. 食材リスト/買い物リストの刷新(フェーズ4)

### 10.1 用語整理

- **食材リスト**: レシピ選択〜不足食材の計算〜編集までの「下書き」。確定するまでDBには保存しない(画面上のstateのみ)。
- **買い物リスト**: 食材リストを確定して保存したもの。家族全体で共有され、購入済みチェックができる。1人あたり最大3件まで作成可能。

### 10.2 食材リスト作成フロー(`/shopping-list`)

1. レシピ一覧をチェックボックスで選択すると、選択レシピの材料を集計した「必要な食材一覧」が**画面上でリアルタイムに**再計算・表示される(サーバー往復なし、クライアント側で計算)。
2. 「必要な食材一覧」の各行に「手持ち数量」の入力欄を表示。自由文ではなく、この一覧に対する数量入力のみで手持ち食材を申告する。
3. 「食材リストを作成」を押すと、必要数量−手持ち数量(0未満は除外)で不足分を算出し、食材リスト(下書き)に遷移する。
4. 食材リスト(下書き)では、各食材の名前・数量・単位・カテゴリを直接編集でき、追加・削除も可能。カテゴリ見出しはドラッグ&ドロップで並び替え可能(野菜・肉・魚など)。
5. 「買い物リストとして確定する」で`shopping_lists`・`shopping_list_items`に保存する。

照合(必要数量−手持ち数量)はAIを使わず決定的なコードで計算する。手持ち食材の入力が構造化されたため、自然文解釈が不要になったための変更(フェーズ2時点のAIエージェントより高速・無料・結果が安定)。

### 10.3 データベース設計

#### shopping_lists

| カラム名 | 型 | 内容 |
|---|---|---|
| id | UUID | 主キー |
| family_id | UUID | families への外部キー |
| created_by | UUID | 作成者(profiles）への外部キー。1人あたり最大3件の上限判定に使用 |
| title | text | 自動生成タイトル(例:「9月19日の買い物リスト」) |
| created_at | timestamp | 作成日時 |

#### shopping_list_items

| カラム名 | 型 | 内容 |
|---|---|---|
| id | UUID | 主キー |
| shopping_list_id | UUID | shopping_lists への外部キー |
| name | text | 食材名 |
| quantity | numeric | 数量(不明な場合はnull) |
| unit | text | 単位 |
| category | text | カテゴリ(未設定時は「未分類」) |
| position | int | 表示順(カテゴリ並び替え結果を反映した確定時点の並び) |
| is_checked | boolean | 購入済みフラグ |

カテゴリはレシピ材料と同じ `ingredients_master.category` を参照し、食材リスト編集時にユーザーがカテゴリを修正すると `ingredients_master.category` にも反映される(学習して次回以降のカテゴリ分けに活用)。

### 10.4 買い物リストの利用(`/shopping-lists`, `/shopping-lists/[id]`)

- `/shopping-lists`: 家族の買い物リストを一覧表示(作成者・購入済み件数を表示)。誰でも新規作成・削除ができる
- `/shopping-lists/[id]`: カテゴリごとに食材を表示。タップすると購入済み(取り消し線)を切り替えられる。家族の誰がチェックしても全員の画面に反映される
- 作成上限(1人3件)は `created_by` ごとにサーバー側でカウントして判定。上限に達した場合はエラーメッセージを表示し、先に不要なリストの削除を促す

### 10.5 二重送信防止

サーバーアクションを呼ぶボタン(送信・削除・いいね・チェックオフなど)は、`useFormStatus` または `useTransition` の pending 状態でボタンを無効化し、処理中はラベルを変更する(例:「保存中...」「削除中...」)ことで、連打による二重登録・二重処理を防止する。`RecipeForm` の送信ボタンは共通コンポーネント `SubmitButton`(`useFormStatus`使用)に統一した。

## 11. 一覧UX改善・献立作成(フェーズ5)

### 11.1 レシピ一覧の改善(`/recipes`)

- **ジャンル別タブ**: 「ジャンル」(主食/主菜/副菜/汁物、それ以外は「その他」)でタブ分け。`src/lib/recipe-genre.ts` の `bucketGenre()` で分類する
- **カテゴリの色分け**: 「カテゴリ」(和食/洋食/中華など)ごとに色バッジを表示。和食/洋食/中華/韓国はプリセット色、それ以外は名前のハッシュ値から決定的に色を割り当てる(`src/lib/category-color.ts`)
- **検索/フィルタ**: 料理名・食材名でのテキスト検索、カテゴリ絞り込み、お気に入りフィルタをクライアント側でリアルタイムに適用(`RecipeListClient`)

### 11.2 レシピ提案と献立提案の役割分担

要望が重複しやすい2機能を、目的で明確に分離した。

| | 新レシピ提案(`/recipe-suggestions`、旧レシピ提案/メニュー提案) | 献立提案(`/menu-plan`、旧献立作成) |
|---|---|---|
| 目的 | 一品を新しく思いつく(発見。既存レシピとの重複回避のみ考慮) | 主食+主菜+副菜+汁物などの「食事セット」を組む(登録済みレシピも活用) |
| 入力 | 自由文(気分・食材・季節など) | 品目ごとの品数(主食/主菜/副菜/汁物を個別に指定、例: 主菜2品)、条件(自由文) |
| 出力 | AI新規案のみを複数フラット表示 | 品目ごとに1件ずつ(登録済み or AI新規案)、"1セット"として提案 |

※フェーズ5では新レシピ提案も登録済み候補を表示していたが、フェーズ6で「新しい提案のみ」に変更した(既存レシピは重複回避のための参考情報としてAIに渡すのみ)。

両画面で「AI新規案カード」(`NewRecipeIdeaCard`、展開でRecipeFormが表示され即登録可能)を共通コンポーネントとして再利用している。献立提案では既存レシピ候補カード(`ExistingRecipeSuggestionCard`)も使用する。

献立提案では、事前に人数を入力させず、品目ごとの品数のみを指定する(人数調整は次工程の食材リスト作成画面で行う)。確定した献立のうち、既に登録済みのレシピはIDを引き継いで `/shopping-list?recipes=id1,id2,...` へ遷移し、食材リスト作成の「レシピを選択」が自動でチェック済みの状態になる画面独自の直接ボタンを持つ。フェーズ6では、これに加えて後述の「献立トレイ」経由でも同様の連携ができるようになった(未登録の新規案も含めて連携可能)。

### 11.3 食材リストを作成の改善(`/shopping-list`)

- **レシピ選択のタブ分け**: レシピ一覧と同じ`bucketGenre()`でタブ分け
- **人数調整**: 選択したレシピごとに「人前」を指定可能。レシピ本来の人数(recipes.servings)との比率で、必要な食材の数量をリアルタイムにスケーリングする
- **必要な食材のカテゴリ別表示**: `ingredients_master.category` を使ってカテゴリごとにグルーピング表示
- **手持ち食材の入力方式変更**: 数量入力オンリーから「チェック+任意の分量入力」に変更。
  - 未チェック → 全量が必要(従来通り)
  - チェック+分量未入力 → 「足りている」とみなし、その食材は食材リストに含めない
  - チェック+分量入力 → 必要量との差分のみ食材リストに含める(不足がなければ含めない)

## 12. 献立トレイ・食材整理(フェーズ6)

### 12.1 新レシピ提案(`/recipe-suggestions`)の刷新

登録済みレシピ候補の表示を廃止し、AIによる新規レシピ案のみを提示する。登録済みレシピ一覧は「重複を避けるための参考情報」としてプロンプトに渡すのみで、UIには表示しない。

### 12.2 献立提案(`/menu-plan`)の改善

- 品目(主食/主菜/副菜/汁物)ごとに品数を個別指定できるようにした(例: 主菜2品)。同一品目が複数の場合はサーバー側で「主菜1」「主菜2」のようにラベルを連番化し、AIにはその通りの品目ラベルで1品ずつ出力させる
- 事前の人数入力を廃止。人数はレシピが確定してから食材リスト作成画面で調整する運用に統一した

### 12.3 献立トレイ

`/recipes`・`/recipe-suggestions`・`/menu-plan` の3画面に、画面右(モバイル幅では下)に常設される「献立トレイ」パネルを追加した。

- **データの保存先**: このブラウザの`localStorage`のみ(`src/lib/meal-plan-tray/context.tsx`、React Contextで全画面から参照)。家族間では共有されない、個人の一時的な作業スペースという位置づけ
- **枠の指定**: 主食/主菜/副菜/汁物ごとに品数(0〜5)を指定すると、その数だけ枠が表示される
- **設定方法**: レシピ一覧のカード、新レシピ提案・献立提案のカード(登録済み・AI新規案とも)を、ネイティブのHTML5 Drag and Dropでトレイの枠にドラッグする。ドラッグデータは`src/lib/meal-plan-tray/drag.ts`でJSONシリアライズして受け渡す
- **人数**: 枠ごとに個別に「人前」を指定できる
- **食材リストへの連携**: 「食材リストを作成へ進む」を押すと、
  1. 枠の中にAI新規案(未登録)が含まれる場合は確認ダイアログを表示
  2. 確認後、未登録の枠は `registerRecipeIdea`(フォームを介さずレシピを登録しIDを返す専用アクション、`src/app/recipes/actions.ts`)で登録
  3. 全ての枠の `recipe_id:人前` の組を `/shopping-list?plan=id1:2,id2:4,...` に付けて遷移し、食材リスト作成画面がその通りに選択・人数設定済みの状態で開く

### 12.4 食材リストを作成のレシピ選択欄の改善

- お気に入り(いいね)したレシピが一覧の先頭に来るよう並び替え
- レシピ一覧と同じカテゴリ色バッジを表示
- 一覧を`max-h-72 overflow-y-auto`のスクロール領域にし、縦に間延びしないようにした

### 12.5 食材名の表記ゆれ対策

レシピによって食材名の表記(商品名・銘柄・パッケージ表記など)が異なると、`ingredients_master`に本来同じ食材を指す行が複数でき、「必要な食材」で同じ食材が別々に表示されたり、カテゴリが常に「未分類」になる問題があった。これをAIで解消する。

**保存時の自動解決(`src/lib/ingredients/resolve.ts`)**: レシピ登録・編集時、材料名を`ingredients_master`に保存する際、まず既存マスタと完全一致するかを確認する(一致すればAI呼び出し不要)。一致しない新規名だけをまとめてClaudeに渡し、「既存のどれかと同じ食材ならその名前に統一する」「新規なら簡潔な正式名とカテゴリを提案する」形で解決する。1回のレシピ保存につきAI呼び出しは高々1回。

**既存データの一括整理(`/ingredients`, 家族設定画面からリンク)**: `ingredients_master`の全件をAIでクラスタリングし、同じ食材を指すグループごとに正規名・カテゴリを決定してマージする(`src/app/ingredients/actions.ts`)。

- 全件を一度に1回のAI呼び出しに渡すと出力が長大になり、Anthropic SDKが「ストリーミング必須」と判定して失敗したり、モデルの思考トークンで予算を使い切りJSON出力が途中で切れる問題が起きたため、60件ずつのチャンクに分割して複数回AI呼び出しを行う(`CHUNK_SIZE`)
- `ingredients_master`は家族を跨いだ共有辞書だが、`recipe_ingredients`は家族単位のRLSで保護されているため、他家族がまだ参照している重複行は付け替え・削除が失敗する(想定内)。失敗した場合はその1件をスキップして処理を続行する(全体を止めない)
- 実行には数十秒〜数分かかるため、`/ingredients`ページに `export const maxDuration = 300` を設定し、Vercel上でのタイムアウトを緩和している(Hobbyプランでは60秒が上限のため、件数が多い家族では別途対応が必要になる場合がある)
- `ingredients_master`への削除操作を許可するRLSポリシーを追加した(migration 0006)

## 13. 管理者機能・セキュリティ対策(フェーズ7)

### 13.1 管理者ロール

`profiles`テーブルに`is_admin`・`is_suspended`の2列を追加(migration 0007)。管理者権限の付与はアプリのUIからは行わず、Supabase SQL Editorから直接`UPDATE`する運用とする(実装複雑化・誤操作防止のため)。

```sql
update public.profiles set is_admin = true where email = '対象のメールアドレス';
```

- 判定は`is_family_member()`等と同じ設計の`SECURITY DEFINER`関数`is_admin()`で行い、RLSポリシー内での自己参照的な再帰を避ける
- 管理者向けRLSポリシーを`recipes`・`recipe_ingredients`・`profiles`・`families`・`family_members`に追加し、家族の垣根を越えた参照・更新(一部削除)を許可する
- **権限昇格の防止**: 一般ユーザーの「自分のプロフィールを更新できる」ポリシーには列単位の制限がないため、悪意あるクライアントから直接`is_admin`/`is_suspended`を書き換えられる余地があった。`profiles`に`BEFORE UPDATE`トリガー(`protect_profile_admin_fields`)を追加し、更新実行者が管理者でない場合はこの2列を常に元の値に戻すことで、RLSだけに頼らない多層防御とした
- アプリ層でも`src/lib/admin/current.ts`の`requireAdmin()`で管理者チェックを行い、非管理者には日本語のエラーメッセージ(「管理者権限が必要です」)を表示する。RLSが最終防衛線、`requireAdmin()`はUXと多層防御の両方が目的
- **不具合の修正(migration 0008)**: 0007のトリガーは「`auth.uid()`から見て管理者でなければ元に戻す」実装だったため、SQL EditorからのUPDATE(PostgRESTのJWTコンテキストを経由せず`auth.uid()`が`null`になる)まで巻き戻してしまい、想定していた「SQL Editorから管理者を付与する」運用そのものが機能しないバグがあった。`auth.uid() is not null and not is_admin()`の場合のみ書き換えを禁止するよう修正した(通常のアプリ利用者は必ずJWTを伴うため`auth.uid()`が`null`になることはなく、保護対象である「一般ユーザーによるPostgREST経由の自己昇格」は引き続き防がれる)

### 13.2 管理者画面

| 画面 | 内容 |
|---|---|
| `/admin` | 管理者ダッシュボード。各管理画面へのリンク |
| `/admin/recipes` | 全家族の全レシピを一覧(所属家族名を表示)。詳細・編集は既存の`/recipes/[id]`・`/recipes/[id]/edit`をそのまま利用(RLSにより権限が自動的に及ぶため、レシピ側のコードは無改修) |
| `/admin/users` | 全ユーザー一覧。表示名の修正、利用停止/解除の切り替えが可能 |

### 13.3 利用停止(アカウント削除は実装しない)

当初はユーザー削除機能(Supabase Authアカウントの完全削除)を検討したが、これには`service_role`キーの追加導入が必要となるため、ユーザーの判断により**削除機能は実装せず、ログイン不可にする「利用停止」制御のみを追加する**方針に変更した。

- `toggleUserSuspension`(`src/app/admin/actions.ts`)で`profiles.is_suspended`を切り替える。管理者が自分自身を停止することは不可
- 実際のログインブロックは`src/lib/supabase/middleware.ts`(全リクエストで実行される`updateSession`)で行う。ログイン済みユーザーが`is_suspended = true`の場合、その場で`supabase.auth.signOut()`しセッションを強制終了したうえで`/suspended`へリダイレクトする。Server Actionの呼び出しも同じミドルウェアを通るため、停止後は次のリクエスト(画面遷移・フォーム送信いずれも)で確実に締め出される
- `/suspended`はログイン不要の公開ページとして、停止された旨と`/login`への導線のみを表示する

### 13.4 Webアプリ基本セキュリティ対策(標準実装ポリシー)

ユーザーの要望により、今後も標準で維持する基本方針として以下を実装した。

- **SQLインジェクション**: 全クエリがSupabaseクライアント経由のパラメータ化クエリであり、生SQLを文字列結合で組み立てている箇所はアプリ本体にはない。唯一の例外である`scripts/bulk-import-recipes.mjs`(ローカルで一度だけ実行する一括登録スクリプト)は、生成するSQL文中の値を`sqlEscape()`でエスケープ済み
- **XSS**: `dangerouslySetInnerHTML`・`eval`・`new Function`はコードベース上に存在しないことを確認済み。Reactのデフォルトのエスケープに任せる
- **セキュリティヘッダー**(`next.config.ts`の`headers()`): 全ルートに`X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Referrer-Policy: strict-origin-when-cross-origin`、`Permissions-Policy`(カメラ・マイク・位置情報を無効化)を付与
- **ファイルアップロード検証**(`uploadPhotoIfProvided`、`src/app/recipes/actions.ts`): 許可するContent-Typeを`image/jpeg`・`image/png`・`image/webp`・`image/gif`に限定(SVGは埋め込みスクリプトによる蓄積型XSSのリスクがあるため除外)。ファイルサイズは5MBを上限とする。保存先の拡張子はユーザー指定のファイル名からではなく、検証済みContent-Typeから機械的に決定する
- **既存の対策の再確認**: レシピURL自動登録機能のSSRF対策(http/https以外・ローカル/プライベートIP拒否、9.2節)、Storageのパスを家族IDでスコープする仕組み(8.3節)は本フェーズでも変更なし

## 14. 食材リストのUX改善・単位表記ゆれ吸収(フェーズ8)

### 14.1 単位のバラつきの吸収(`src/lib/ingredients/units.ts`)

食材名は12.5節でAIにより表記ゆれを吸収済みだが、同じ食材でもレシピによって単位が異なる(人参の「240g」と「1本」、みりんの「大さじ」と「小さじ」)場合、「必要な食材」に同じ食材が複数行に分かれて表示される問題が残っていた。

- g/kg(重量)、ml/cc/L/大さじ/小さじ/カップ(体積)は換算可能な単位としてまとめ、合算後はg/ml(1000以上ならkg/L)に統一して1つの数量として表示する。大さじ・小さじの表記には戻さない(複数レシピ分を合算した後はさじ数に戻すとかえって分かりにくいため)
- 「1本」「1個」のような個数単位は食材ごとに重さが異なり機械的な換算ができないため対象外とし、換算できない単位はそのまま個別の数量として保持する
- 表示は食材名を1行にまとめたうえで、その下に単位ごとの数量(セグメント)を並べる形にした(例:「人参」の下に「必要: 240g」と「必要: 1本」を別々に表示。合算できるものは1本化され、できないものは同じ食材の下にまとまる)
- 手持ちチェック・分量入力はセグメント単位で行う(1本は持っているが追加のgは持っていない、といったケースに対応するため)

### 14.2 カテゴリの一括「持っている」選択

調味料など「大抵は自宅にある」食材を1つずつチェックする手間を減らすため、「必要な食材」の各カテゴリ見出しに「すべて持っている」チェックボックスを追加した。カテゴリ内の全セグメントを一括でチェック(分量未入力=足りているものとして扱われ、食材リストから除外される)・解除できる。

### 14.3 スーパー準拠のカテゴリ体系への変更(`src/lib/ingredients/categories.ts`)

従来のカテゴリ(野菜/肉・魚/調味料/乳製品・卵/主食/その他)は粒度が粗く、「小麦粉薄力タイプ」が「主食」に分類されるなど、実際の買い物と噛み合わない例があった。スーパーの売り場構成に近い形に変更した。

```
野菜・果物 / 肉 / 魚介 / 卵・乳製品 / 豆腐・大豆製品 / 米・パン・麺 / 調味料・油 / 粉類・乾物・缶詰 / 冷凍食品 / その他
```

- 新規保存時の自動分類(`resolve.ts`)・既存データの一括整理(`ingredients/actions.ts`)双方のAIプロンプトをこのカテゴリ一覧に差し替えた
- 一括整理(`/ingredients`)はカテゴリ再分類も兼ねているため、**既存データにこのカテゴリ体系を適用するには一括整理を再実行する必要がある**(移行用の別マイグレーションは用意していない)。実データ(436件)で再実行し、360件に整理・334件のカテゴリを更新したことを確認済み

### 14.4 食材リストを作成画面のレシピ選択欄の折りたたみ

献立トレイやレシピ一覧など、レシピが選択済みの状態(`?plan=`または`?recipes=`付き)でこの画面に遷移した場合、レシピ選択欄を冗長に感じるとの指摘を受け、その場合は「◯件のレシピを選択中です。変更する」という折りたたみ表示に変更した。選択なしで直接アクセスした場合(ナビゲーションの「食材リストを作成」など)は従来通り開いた状態で表示する。

### 14.5 今後の追加改善案(未実装・提案)

「忙しい主婦層向けに、直感的かつ簡易な操作で献立から買い物リストまで進められること」という方針に沿って、今回は着手していないが効果が見込める改善案:

- 画面上部に「① 献立を選ぶ → ② 食材を確認 → ③ 買い物リスト」のようなステップ表示を付け、迷わず進められるようにする
- 献立トレイ→食材リスト→買い物リストの一連の流れをワンタップで完了できる「今日はこれで買い物リストを作る」のようなショートカット動線
- 買い物リスト画面(チェックリスト)をスーパーの売り場順(カテゴリ順)で固定表示し、店内を一筆書きで回れるようにする
- 必要な食材の数量が僅少(「少々」「適量」)なものは「必要:」の代わりに控えめな表示にし、実質的に確認不要な項目だと分かるようにする

## 15. UI/UXリニューアル・AI利用最適化(フェーズ9)

14.5節の改善案をすべて実装し、加えてブランディング刷新とAI関連の細かい使い勝手改善を行った。

### 15.1 ブランディング刷新

- サービス名を「自炊レシピ管理」から「レシピマネージャー」に変更(`<title>`・ヘッダーロゴ)
- ロゴアイコンを絵文字からコック帽のSVGアイコンに変更し、`src/app/icon.svg`としてファビコンにも採用
- 配色をエメラルドグリーンからオレンジ/コーラル系に変更。Tailwind v4の`@theme`でカスタムカラースケール`brand-*`(`src/app/globals.css`)を定義し、アプリ全体の`emerald-*`クラスを`brand-*`に置き換えた
- レシピのジャンル別カラーバッジ(`category-color.ts`、和食=emerald等の8色パレット)はブランドカラーとは独立した別の色分けのため、意図的に対象外とした

### 15.2 食材リストを作成画面の改善(14.5の実装)

- 画面上部に「①献立を選ぶ→②食材を確認→③買い物リスト」のステップ表示を追加(`IngredientListBuilder`内`StepIndicator`)。現在地は選択済みレシピ数と画面状態から自動判定する
- 「今日はこれで買い物リストを作る」ボタンを追加。手持ち確認を省略し、必要な食材をそのまま(スーパーの売り場順に並べて)買い物リストとして確定する近道
- 買い物リスト(`/shopping-lists/[id]`)のカテゴリ表示順を、保存時のposition順ではなく常にスーパーの売り場順(E章のカテゴリ順)に固定した
- 必要な食材の数量が「少々」「適量」等(数量なし)の場合、「必要:」を付けず控えめな文字色・小さめのフォントで表示するようにした

### 15.3 献立トレイのタップ操作対応

スマホではドラッグ&ドロップの操作性が悪いという指摘を受け、レシピ一覧・新レシピ提案・献立提案の各カードに「+ 献立トレイに追加」ボタンを追加した(`AddToTrayButton`)。

- タップすると、そのレシピのジャンルに対応する品目の空き枠に自動で追加される。ジャンルが「その他」等トレイの4品目に当てはまらない場合は「主菜」を既定の置き先とする(`toTrayGenre()`)
- 空き枠が無い場合は、その品目の上限(5枠)内であれば新しい枠を自動で追加してから設定する。上限に達している場合はボタンに「この品目は満杯です」と表示する
- 献立トレイのcontext(`meal-plan-tray/context.tsx`)に`addToTray()`を追加し、既存のドラッグ&ドロップ(`assign()`)とは別経路として共存させた。ドラッグ操作自体は変更していない(PC利用時はそのまま使える)

### 15.4 AI待機中の体感改善

- 「押した感じがしない」という指摘に対し、主要なボタンに`active:scale-95`+背景色の濃い`active`状態+`transition`を追加し、押下時に沈み込むような視覚フィードバックを付けた(共通の`SubmitButton`と各フォームの送信ボタンに適用)
- AI応答待ち(新レシピ提案・献立提案・レシピURL自動インポート)に、動きのある「調理中」インジケーター(`AIThinkingIndicator`。鍋の絵文字が弾み、湯気が立ち上るCSSアニメーション)を表示するようにした。単なるスピナーよりアプリの世界観に合わせた

### 15.5 AI応答時間について(調査結果・現状の判断)

新レシピ提案・献立提案は`claude-opus-5`(effort: medium)を使っており、数秒〜十数秒程度の応答時間がかかる。これは主に(1)フロンティア級モデルの推論時間、(2)構造化出力(レシピ名・材料・手順を含む比較的大きなJSON)の生成時間、によるもの。

- 対応済み: 15.4のアニメーション表示により、体感的な「待たされている感」を緩和した
- 今回は見送った選択肢: `effort`を`medium`から`low`に下げれば応答は速くなるが、提案の質(妥当性・具体性)が下がるトレードオフがある。ストリーミングで部分的な結果を逐次表示する方式も技術的には可能だが、構造化出力(Zodスキーマ検証)は完全なJSONが揃うまで検証できないため、実装コストの割に効果が薄いと判断し見送った
- 食材名解決・URL抽出は15.6の通り`claude-haiku-4-5`に切り替えたことで、実測はしていないが体感の応答速度は上がっている可能性が高い

### 15.6 買い物リストのクリップボードコピー

`/shopping-lists/[id]`に「コピーする(LINEなどに貼り付け用)」ボタンを追加した(`ShoppingListChecklist`)。カテゴリ見出し+品目名+数量のプレーンテキストを`navigator.clipboard.writeText()`で生成・コピーする(招待コードコピー機能と同じ実装パターン)。

### 15.7 一括整理ツールの対象条件を修正(フェーズ8で入れた増分処理の穴埋め)

フェーズ8で「未分類(category IS NULL)の行のみを対象にする」増分処理に変更したが、フェーズ8のカテゴリ体系変更以前に旧カテゴリ値(例:「調味料」)が設定されたまま残っている行は、NULLではないため対象から漏れることが実データ確認で判明した。対象条件を「NULL、または現在の10分類(E章)に含まれない値」に修正し、旧タクソノミーの取りこぼしも拾えるようにした(`src/app/ingredients/actions.ts`)。

**既知の残課題**: この増分方式は「未分類・旧タクソノミー」の取りこぼしは拾えるが、既に有効なカテゴリが設定済みの行同士の表記ゆれ(例:ブランド名入りの「マンジョウ国産米 本みりん」が汎用の「みりん」に未統合のまま残るケース)までは拾わない。全件を毎回スキャンしていたフェーズ8以前の方式ならいずれ拾えたが、コスト最適化とのトレードオフとして許容している。気になる場合は、全件を対象にした「徹底整理モード」を別途追加することも可能(未実装)。

## 16. デザインシステム刷新・不具合修正(フェーズ10)

「料理をしたくなるような背景色にしたい」「各機能に遷移したときにパッと見てどの機能か分かるようにしたい」という要望を受け、Artifactでの配色モックアップ提示・承認を経て実装した。

### 16.1 背景色・ゾーン別カラーコーディング

- 背景色を白から生成り(きなり)・まな板色(`--color-paper: #faf3e6`)に変更(`src/app/globals.css`のTailwind v4 `@theme`)
- 機能ごとに5つの「ゾーン」を定義(`src/lib/zones.ts`): `recipe`(レシピ探す・登録、🍳)/`ai`(AIに考えてもらう、✨・紫)/`shopping`(食材・買い物リスト、🛒・緑)/`family`(家族管理、👪・ティール)/`admin`(管理者専用、🛡️・スレート)。各ゾーンはアイコン・背景色・文字色・ラベルを持つ
- `ZoneIcon`コンポーネントでゾーンごとの配色バッジを共通化し、各機能ページの見出しと`HeaderNav`の現在地表示の両方で使用。操作ボタンの色(brand)自体はどのゾーンでも統一し、ゾーン色は「今どの機能にいるか」の識別のみに使う
- レシピのジャンル別カラーバッジ(`category-color.ts`)はこの5ゾーンとは独立した別の色分けのため対象外(15.1と同じ整理)

### 16.2 ホーム画面の刷新

- 「おかえりなさい」の挨拶文を削除
- 「①レシピを選ぶ→②献立トレイに集める→③食材を確認する→④買い物リストが完成」という利用フローを、アイコン付きのステップ帯として表示(`FLOW_STEPS`)
- 各機能への導線を、ゾーンアイコン付きのカードグリッドに刷新(`ZoneIcon`を使用)

### 16.3 コンテンツシャドウ

Tailwind v4の`@theme`にカスタムシャドウトークン`--shadow-raised`(通常カード用)・`--shadow-brand`(主要ボタン用)を定義し、白背景カード・ボタンに適用。背景色が白でなくなったことで、カードが「紙の上に浮いている」ように見えるようにした。

### 16.4 不具合修正: 背景色が反映されないバグ(CSSカスケードレイヤー)

ローカルで確認したところ、16.1の変更後も背景が白のままに見えるバグが発生。原因はNext.jsテンプレート由来の`body { background: var(--background); color: var(--foreground); font-family: Arial, ...; }`という素のCSSルールが`globals.css`に残っており、どの`@layer`にも属していなかったこと。CSSカスケードレイヤーの仕様上、レイヤー外のルールは詳細度に関わらずレイヤー内のルール(Tailwindが生成するユーティリティは`@layer utilities`に属する)に常に勝つため、`<body>`の`bg-paper`ユーティリティが無効化されていた。このルールと、既に死んでいた`--background`/`--foreground`変数・ダークモード用メディアクエリを削除して解決。副次的に、この`font-family: Arial`指定が既に読み込み済みだったGeistフォントを終始上書きしていたことも判明し、`<body>`に`font-sans`を付与して修正した。

### 16.5 不具合修正: 「+献立トレイに追加」ボタンのはみ出し(2件・別原因)

15.3で追加したボタンについて、2種類の独立したはみ出し不具合が見つかり、それぞれ別原因だった。

1. **横方向のはみ出し**: ボタンの位置指定が`absolute bottom-3 right-3`(左側が無制限)で、かつ「この品目は満杯です」等の動的ラベルが長い場合にカード幅を超えてはみ出していた。ボタンラベルを短縮(「+ トレイに追加」/「追加しました」/「満杯です」)し、位置指定を`absolute inset-x-3 bottom-3 flex justify-end`に変更、`max-w-full truncate`を追加して解決
2. **縦方向のはみ出し(写真なしカード等で発生)**: CSS Gridの既定`align-items: stretch`により、`<li>`(ボタンの位置基準)は行の高さいっぱいに伸びる一方、中の`<Link>`(見た目上の白いカード)は伸びないため、ボタンが視覚的なカードの外(下)にずれて見えていた。`<Link>`に`h-full`を追加し、カードの見た目の高さをリンク自身が埋めるようにして解決(`RecipeListClient.tsx`)

### 16.6 ツールチップの追加

主要なボタン・機能に`title`属性を追加し、ホバー時に簡単な説明が出るようにした。

### 16.7 不具合修正: 献立トレイの品数を減らすとレシピが無警告で消える(データ損失)

「どこまでテストされているか」という確認依頼を受けた手動テスト監査で発見。ある品目(例: 副菜)の枠数を3→1のように減らすと、`rebuildGenreSlots()`が末尾側の枠を確認なしに破棄しており、そこに設定済みだったレシピが警告なく失われていた。`MealPlanTrayPanel`に`handleCountChange()`を追加し、減らす枠のいずれかにレシピが設定済みの場合は`window.confirm()`で警告してから実行するように修正した。

## 17. 献立エージェント(フェーズ11)

### 17.1 背景・目的

既存のAI機能(新レシピ提案・献立提案)は「1往復で終わる」単発の生成であり、本来の意味でのAIエージェント(外部ツールと連携し、自律的に一連の業務を遂行する)とは言えないのではという相談を受けた。設計案を提示・議論した結果、既存機能とは別建てで、会話をしながら「献立を決める→食材を確認する→買い物リストを作成する」という一連の流れそのものを遂行する、管理者限定の実験的なチャット型エージェント機能を新設することにした(既存の`新レシピ提案`/`献立提案`/`食材リストを作成`は無変更)。

管理者はこのアプリでは本人のみであるため、アクセス制御は単純に`requireAdmin()`のみとした。

### 17.2 アーキテクチャ: 2段構成によるコスト制御

1. **門番(ゲート)チェック**: `claude-haiku-4-5-20251001`による構造化出力(`{in_scope, redirect_message}`)。直近の会話履歴込みで、そのユーザー発言が献立・食材・買い物リストの相談として妥当かを判定する。対象外(料理と無関係な話題)の場合は、高コストな本番ループに入らずその場で案内メッセージを返す
2. **本体ループ**: `claude-opus-5`によるツール呼び出しループ(最大8ラウンド)。会話の流れを見ながら、次に何をすべきか(人数・ジャンルの確認、登録済みレシピの検索、無ければ新規提案、献立プランへの追加、食材集計、買い物リスト下書き)を能動的に提案・実行する。システムプロンプトで「献立決定→食材確認→買い物リスト作成」という目的への誘導を明示している

### 17.3 ツール(6種、`src/lib/anthropic/menu-agent-tools.ts`)

| ツール | 内容 | 書き込み |
|---|---|---|
| `search_recipes` | 登録済みレシピをキーワード/品目/カテゴリで検索(家族スコープ) | 読み取りのみ |
| `get_recipe_details` | レシピ詳細(材料・手順)を取得 | 読み取りのみ |
| `propose_new_recipe` | 登録済みに適切な候補が無い場合、新しいレシピ案を作成 | DB書き込みなし(会話内の一時的な案として保持) |
| `add_to_meal_plan` | 検索/提案したレシピを、会話スコープの献立プランに追加 | サーバーメモリ上のプラン状態のみ |
| `compute_shopping_list` | 現在の献立プランから必要な食材を集計(単位統合・カテゴリ整理は`src/lib/ingredients/aggregate.ts`を共用) | 読み取り・計算のみ |
| `create_shopping_list` | 買い物リストの下書き(除外食材の反映込み)を提示 | DB書き込みなし(下書き提示のみ) |

### 17.4 書き込みの安全設計: 「モデルの言うことを信用せず、仕組みとして止める」

`propose_new_recipe`によるレシピ登録、および買い物リストの実際の保存は、いずれもモデルのツール呼び出しだけでは実行されない設計とした。

- レシピ登録: `propose_new_recipe`はDBに一切書き込まず、会話内の一時案(`idea_id`で参照)を返すのみ。実際の登録は、画面の「登録して進める」ボタンから、クライアント側で既存のServer Action `registerRecipeIdea`(`src/app/recipes/actions.ts`、フェーズ9で献立トレイ向けに実装済みのもの)を直接呼び出すことで行う
- 買い物リスト確定: `create_shopping_list`はDBに一切書き込まず、下書きを提示するのみ。実際の保存は、画面の「この内容で買い物リストを確定する」ボタンから、クライアント側で既存のServer Action `createShoppingList`(`src/app/shopping-list/actions.ts`)を直接呼び出すことで行う。未登録の新規案が残っている場合は、確定前にまず自動でレシピ登録を済ませる(献立トレイの`handleProceed()`と同じ安全策)

この設計により、モデルが誤った判断をしても実際のデータベース書き込み経路(`registerRecipeIdea`・`createShoppingList`)自体はユーザーの明示的なボタン操作からしか呼び出せない。

### 17.5 UI・会話履歴

- チャットUI(`MenuAgentChat.tsx`)は、ユーザー発言・エージェント発言に加え、実行したツール呼び出しの結果を短い行(行動ログ)として表示する。APIは1トリガーにつきNDJSON形式でイベント(`action`/`message`/`plan`/`shopping_draft`/`out_of_scope`/`done`/`error`)を逐次ストリーミングし、クライアントは受信次第UIに反映する
- 会話履歴と献立プランは、既存の献立トレイ(`meal-plan-tray`)と同じ方針でブラウザの`localStorage`に保存する個人の一時的な作業スペースとした(サーバー・DB永続化は今回見送り)。保存turn数の上限(40、往復20回相当)を設け、コストの際限ない増加を防いでいる。「新しい会話を始める」ボタンで明示的にリセットできる
- `/menu-agent`は全体ナビ(`HeaderNav`)には追加せず、`/admin`ダッシュボードからのみ導線を張った(通常・高速フローを煩雑にしないため)

### 17.6 実装確認で見つけた不具合・調整

- モデルが応答にMarkdown記法(`**太字**`など)を使い、プレーンテキスト表示のチャット吹き出しに記号がそのまま出てしまう問題があり、システムプロンプトに「Markdown記法を使わない」旨を明記して解決
- `create_shopping_list`の`exclude_names`による除外が完全一致だったため、「A 塩」のように手順グループの接頭辞が付いた食材名(元データ由来)が「塩」の指定で除外されない問題があった。部分一致(`includes`)による除外に変更して解決
- 当初のシステムプロンプトでは、新規レシピ案(`propose_new_recipe`)を登録確認の前に献立プランへ追加することをモデルが避ける挙動が見られ、プラン表示に反映されないまま確認だけが先行してしまった。「未登録のまま追加してよく、画面には未登録と表示される」旨を明記し、承認済みモックアップ通り「プラン全体を見せてから登録確認する」流れに修正した

### 17.7 既知の制約・見送った項目

- 会話履歴のサーバー/DB永続化(複数端末での共有、複数スレッド管理)は見送り、v1では単一端末のlocalStorageのみ
- 買い物リスト確定後に「新しい会話を始めますか」と促す任意のプロンプトは実装せず、既存の「新しい会話を始める」ボタンでの明示リセットのみとした
- パントリー(在庫)管理ツールは対象外(フェーズ11時点)。「管理者以外への公開」自体はフェーズ12で個別許可制として実現した(17.7の元記述を更新)

## 18. バージョン管理・買い物リスト上書き・家族の買い物設定・献立エージェント権限付与(フェーズ12)

ユーザーから5件の改修依頼がまとまって来たフェーズ。DBスキーマ変更(`supabase/migrations/0009_menu_agent_access_and_shopping_settings.sql`)を伴う。

### 18.1 デプロイバージョンの表示・強制再ログイン

- `src/lib/version.ts`の`getAppVersion()`が、Vercelが自動注入する`VERCEL_GIT_COMMIT_SHA`(システム環境変数)の先頭7桁を返す。ローカル開発時は未設定のため常に`"dev"`になる
- ログイン画面(`/login`)の下部に`version {getAppVersion()}`を表示
- `src/lib/supabase/middleware.ts`(`updateSession`、既存の`is_suspended`チェックと同じ場所)で、リクエストごとに現在のバージョンと`app_version`Cookieを比較する。ログイン中にバージョンが変わっていた場合はサインアウトし、`/login?reason=updated`にリダイレクトする(ログイン画面に理由メッセージを表示)。Cookieは訪問のたびに現在バージョンへ更新される
- ミドルウェアは全ページ遷移・Server Action呼び出しで実行されるため、クライアント側の常駐ポーリングは不要とした(既存の認証チェックの仕組みをそのまま再利用)

### 18.2 買い物リストの上書き保存・作成日時の表示

- `createShoppingList`(`src/app/shopping-list/actions.ts`)が`options: { storeId?, overwriteListId? }`を受け取れるように変更。`overwriteListId`を指定すると新規作成の代わりに既存リストの品目を全削除→再挿入し、タイトルと`updated_at`を更新する
- 件数上限(1人3件)に達した場合、従来は例外を投げて終了していたが、共有定数`SHOPPING_LIST_LIMIT_MESSAGE`(`src/lib/shopping/messages.ts`)でエラーを判別し、クライアント側(`IngredientListBuilder`・`MenuAgentChat`)が自分の既存リスト一覧(`getMyShoppingListsForOverwrite`)を取得して上書き先を選ばせるUIに切り替える
- `shopping_lists`に`updated_at`(上書き保存時に更新)を追加。一覧画面・詳細画面の両方に作成日時(および更新日時が異なる場合はそれも)を表示するようにした(`formatDateTime`、`src/lib/format-date.ts`)

### 18.3 家族の「どの買い物でも必ず含める食材」

- `family_default_items`テーブル(家族単位、owner のみ編集可、全メンバー閲覧可)を追加し、`/family`に管理UI(`FamilyDefaultItemsSettings`)を設置
- 食材リスト作成画面(`IngredientListBuilder`)・献立エージェント(`compute_shopping_list`/`create_shopping_list`)のどちらでも、必要な食材を集計する直前にこのデフォルト食材を「人数スケーリングなしの疑似レシピ」として`aggregateNeededIngredients`に追加で渡すことで、既存の集計ロジックをそのまま再利用して合算している(`servings: null`のため人数倍率が適用されない)

### 18.4 家族の「よく使うスーパー」(カテゴリの並び順)

- `family_stores`テーブル(家族単位、最大10件はアプリ側で制御、owner のみ編集可)を追加。各スーパーは10分類ぶんの並び順(`category_order`)を保持し、新規追加時は標準順(E章)で初期化される
- `/family`の管理UI(`FamilyStoreSettings`)では、スマホでの操作性を考慮し(フェーズ9で得た「ドラッグ&ドロップは操作性が悪い」という知見を踏襲)、カテゴリの並び替えはドラッグではなく▲▼ボタンにした
- `sortByCategoryOrder`(`src/lib/ingredients/categories.ts`)が任意の並び順配列を第2引数に取れるよう拡張(省略時は従来通り標準10分類順)
- 食材リスト作成画面・献立エージェントのどちらでも、買い物リスト確定時にスーパーを選択できる(未選択時は標準順のまま、挙動は変更なし)。実際の並び替えは`createShoppingList`内で選択されたスーパーの`category_order`を使って行い、`shopping_lists.store_id`に記録する。買い物リスト詳細画面(`ShoppingListChecklist`)は、保存時の`store_id`から並び順を引いて表示する(保存後にスーパー設定を変更しても、そのリストの表示順は保存時の設定を都度参照するため追随する)

### 18.5 献立エージェントの個別権限付与

- `profiles.can_use_menu_agent`を追加。献立エージェントのアクセス制御を`requireAdmin()`から新設の`requireMenuAgentAccess()`(`is_admin`または`can_use_menu_agent`のいずれかでアクセス可)に変更した
- `/admin/users`の各行に、管理者以外のユーザー向けの利用可否トグル(`toggleMenuAgentAccess`)を追加。管理者自身は常に利用可能なため、管理者の行にはトグルを出さず注記のみ表示する
- `is_admin`/`is_suspended`の自己書き換えを防ぐ既存トリガー(`protect_profile_admin_fields`)に`can_use_menu_agent`も追加し、非管理者が自分自身に権限を付与できないようにした(UIだけでなくDBレイヤーでも防止する、フェーズ7以来の設計方針を踏襲)
- 献立エージェントは全体ナビ(`HeaderNav`)・ホーム画面には表示していなかった(フェーズ11時点の判断)が、管理者以外にも利用者が広がり得るようになったため、`is_admin || can_use_menu_agent`のユーザーには両方に導線を表示するよう変更した

### 18.6 実装確認で見つけた不具合

- `"use server"`ディレクティブを持つファイル(Server Actions)は関数以外のエクスポートを許可しない仕様のため、`family/actions.ts`に定数`MAX_FAMILY_STORES`を、`shopping-list/actions.ts`に定数`SHOPPING_LIST_LIMIT_MESSAGE`を直接エクスポートしてビルドエラー(「モジュールにエクスポートが存在しない」)になった。両定数を通常のモジュール(`src/types/shopping-settings.ts`、`src/lib/shopping/messages.ts`)に切り出して解決した

## 19. ビルド日時表示・ホーム画面バナー・遷移中ローディング表示・favicon修正(フェーズ13)

ユーザーからの複数の質問・改修依頼に対応。

### 19.1 買い物リストのチェック操作について(質問への回答)

`ShoppingListChecklist`のチェック操作(タップで取り消し線)は、`toggleShoppingListItem`というServer Action経由でSupabaseの`is_checked`列を更新している。つまり毎回サーバー通信を伴う(ローカルの見た目だけを切り替える実装にはなっていない)。

### 19.2 バージョンにビルド日時を追加

`next.config.ts`の`env`に`BUILD_TIME: new Date().toISOString()`を追加した。`next.config.ts`は`next build`実行時に1回だけ評価されるため、デプロイのビルド日時として使える(Vercelのシステム環境変数にはビルド日時に相当するものが無いため、この方法で代用)。`src/lib/version.ts`の`getBuildTime()`が日本語表記に整形し、ログイン画面にバージョン(コミットSHA)と併記する。

### 19.3 ホーム画面のユースケースバナー

「レシピを選ぶ→…→買い物リストが完成」という単一のフローのみを表示していた帯を、`UsageScenarioBanner`(状況ごとの使い方を4パターン、5秒おきに自動切り替え)に置き換えた。下部のドットをタップして手動切り替えも可能。冗長になる箇条書きではなく、モックアップ提示なしで既存のフロー帯と同じ見た目のまま中身だけ複数化する形にした(デザインの一貫性を優先)。

### 19.4 遷移中のローディング表示

Next.js App Routerの規約ファイル`src/app/loading.tsx`を新設した。ページ遷移でサーバー側のデータ取得を待つ間、Next.jsが自動的にこれを表示する(ボタン押下→次画面表示までの間、レシピマネージャーのアイコンを揺らしながら全画面をグレーアウトする)。既存の`AIThinkingIndicator`と同じ`nod-think`アニメーションを再利用し、追加のJSやイベント配線は不要。

**既知の制約**: `loading.tsx`が効くのはページ遷移(新しいルートのサーバーコンポーネントがデータ取得している間)のみ。Server Actionがボタン押下後、`redirect()`を呼ぶ前の処理中(DB書き込み中など)はこの対象外で、各画面が個別に持つボタンの「登録中...」等のテキスト表示でカバーしている。全操作を横断する統一的な全画面オーバーレイ(Server Action実行中も含む)は今回のスコープでは見送った。

### 19.5 favicon修正

`src/app/`に、Next.jsの初期テンプレート由来の`favicon.ico`(2026-09-17付、未使用)と、フェーズ9で設定した実際のアイコン`icon.png`(2026-09-20付)が両方残っており、これが本番タブのアイコンが意図通りに見えない原因だった可能性が高い。`favicon.ico`を削除し、`icon.png`のみを残した(ブラウザの`<link rel="icon">`が1つだけになることを確認済み)。favicon はブラウザに強くキャッシュされる傾向があるため、デプロイ後も反映されない場合はハード再読み込みやシークレットウィンドウでの確認を推奨する。

### 19.6 独自ドメインの費用について(質問への回答)

Vercelの無料(Hobby)プランでも独自ドメインの追加自体に費用はかからない。ただしドメイン名自体の取得・更新費用(レジストラへの支払い、TLDにより年間数百〜数千円程度)は別途必要。正確な金額はVercelの料金ページ・レジストラの最新情報を確認すること。

### 19.7 買い物リストのチェックをバルク更新化

「チェックのたびにサーバー通信が発生するのは避けたい」という要望を受け、`ShoppingListChecklist`をタップ即時のローカル表示(オプティミスティックUI)+約1.2秒のデバウンス後にまとめて送信、という方式に変更した。

- タップした瞬間に取り消し線をローカル状態(`localChecked`)で即時反映し、変更をメモリ上(`pendingRef`)に溜める
- 最後の操作から約1.2秒操作が無ければ、溜まった変更をまとめて新設のServer Action `bulkUpdateShoppingListItems`(`src/app/shopping-lists/actions.ts`)で1回のリクエストとして送信する。Supabase側は複数件の`update`を`Promise.all`で並列実行しているため、クライアント→サーバーの往復は1回に集約される
- 画面を離れる(コンポーネントのアンマウント)ときは、デバウンス待ちの変更があれば即座に送信して取りこぼしを防ぐ
- サーバーからの新しい`items`(他端末での変更等)が来ても、まだ送信していない項目のローカル表示は上書きしないようにしている(`useEffect`でpendingRef未登録の項目だけ同期)
- コピー ボタンの隣に「まもなく保存します...」「保存中...」「保存しました」を小さく表示し、送信タイミングが分かるようにした

ブラウザの`read_network_requests`で、複数タップが実際に1回のPOSTにまとまることを確認済み。

**通信失敗時の挙動**: 送信が失敗した項目は未送信キューに戻し、5秒後に自動で再送する(送信中に同じ項目が新しくタップされていた場合は、その新しい値を優先し古い値で上書きしない)。「保存に失敗しました。自動で再試行します...」と表示する。ただし画面離脱時(アンマウント)の即時送信が失敗した場合は、ユーザーは既に別画面へ移っているため再試行できない(次にその買い物リストを開いたときに、サーバー側の実際の状態が表示される)。

### 19.8 画面ごとのヘルプ機能

相談の結果、ホバー前提の`title`ツールチップはスマートフォンでほぼ機能しない(フェーズ9で得た知見)ため、**各画面の見出し横に「?」ボタンを1つ配置し、タップでその画面の使い方をまとめて表示する**方式(`HelpPanel`)を採用した。

- スマホでは画面下からのシート、PC相当の幅では中央モーダルとして表示される(同一コンポーネントで両対応)
- ホーム・レシピ一覧・新レシピ提案・献立提案・食材リストを作成・買い物リスト一覧・家族設定・献立エージェントの各画面に導入した(買い物リスト詳細・管理者向け画面は対象外。詳細画面は既存の「タップすると購入済みにできます」という案内で足りると判断した)
- 既存のボタンの`title`属性(PCでのホバー時の補助)はそのまま残しており、両者は排他ではなく併用している
